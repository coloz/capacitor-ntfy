import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.coloz.capacitor.ntfy.demo',
  appName: 'Capacitor ntfy Demo',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
